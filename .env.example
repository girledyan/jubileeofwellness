# Postgres connection string — a free Neon (https://neon.tech) or Vercel
# Postgres project is the fastest way to get one with no local install.
DATABASE_URL="postgresql://user:password@localhost:5432/jubileeofwellness"

# Shared secret to view signups at /admin?key=... — pick a long random string.
ADMIN_KEY="change-me"
